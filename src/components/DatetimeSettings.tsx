import React from 'react';
import { IServiceProps } from './IServiceProps';

const DatetimeSettings: React.FC<IServiceProps> = (props: IServiceProps) => {
  return <div>Datetime Settings</div>;
};

export default DatetimeSettings;
